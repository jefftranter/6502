10 REM SHOW TIME FROM INTERRUPT ROUTINE.
20 REM NEED TO RUN RTC BEFORE RUNNING THIS.
30 H = PEEK(28928)
40 M = PEEK(28929)
50 S = PEEK(28930)
60 PRINT H;M;S
110 IF S = PEEK(28930) THEN GOTO 110
120 GOTO 30
130 END
